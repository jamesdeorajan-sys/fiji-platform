# R1 Inventory — bookfijitransfers.com

Storefront role (Issue #37): Fiji-wide marketplace / overflow network.

| Field | Value |
|---|---|
| Live domain | bookfijitransfers.com |
| Cloudflare mapping | **UNKNOWN/REVIEW REQUIRED, reported not guessed.** Confirmed facts only: the zone `bookfijitransfers.com` (zone id `9e809d89568519f193876182feba8d4c`) is a real, active, "full" zone that DOES belong to the same Cloudflare account (`595101df2c562b3c65595420d43f9fe1`) as the other two storefronts — verified directly via the Cloudflare API's own `account.id` field on the zone object, not inferred from shared nameservers. However: it is **not** one of the account's 67 Cloudflare Pages projects (checked all of them by domain); it is **not** a Worker Custom Domain in this account (only `api.nadiairporttransfers.com` and `api.fijidash.com` are registered there); it has **zero** Worker Routes configured on its zone. The zone's DNS records could not be read — the current OAuth session's token returned `Authentication error` (code 10000) on both `dns_records` and `custom_hostnames`, which reads as a scope limitation of this session's credential, not a confirmed "no records" state. **Do not assume Pages/Workers hosting for this property until DNS is actually read with a properly-scoped credential.** |
| Source repo | **UNKNOWN/REVIEW REQUIRED.** Checked all 5 repos under `jamesdeorajan-sys` (`fiji-platform`, `come-to-fiji`, `fijitransferbooking`, `fijiboundless`, `discoverfiji`) for any reference to "bookfijitransfers" — zero hits, including full git history search on `fiji-platform`. The similarly-named repo `fijitransferbooking` is a **different, unrelated property**: its `BUILD_LOG.md` confirms its own custom domain is `fijitransferbooking.com` (not bookfijitransfers.com), it explicitly redirects ground/airport transfers **out** to nadiairporttransfers.com rather than competing with it, and its stack (Next.js 15 + `@cloudflare/next-on-pages`) does not match bookfijitransfers.com's live framework fingerprint. These are two distinct sites that happen to have confusingly similar names. |
| Source branch/commit | UNKNOWN — no source located to have a branch/commit for. |
| Quote endpoint(s) | `POST https://bookfijitransfers.com/api/quotes` — confirmed live and working via direct read-only test calls (this pass). Body: `{destinationId, tripType, pickupDate, pickupTime, returnDate, returnTime, passengers, luggage}` — **no `originId`/pickup field**: this storefront is hub-and-spoke, Nadi Airport is the implicit fixed origin for every route today. Response: `{quoteId (uuid), pricingVersionId, fares: {sedan, minivan, minibus}, currency, expiresAt, pricePromise}`. Quotes are explicitly **locked for a fixed window** (`expiresAt`, ~30 minutes per the on-page copy) and versioned (`pricingVersionId: "baseline-v1"`) — a more formal quote-lock model than either of the other two storefronts has today. |
| Booking create endpoint(s) | `POST https://bookfijitransfers.com/api/bookings` (discovered via passive inspection of the site's own shipped JS bundle — **not exercised**, per this task's strict read-only instruction). Body shape (from source): `{idempotencyKey: crypto.randomUUID(), fareQuoteId, destinationId, tripType, pickupDate, pickupTime, returnDate, returnTime, passengers, luggage, vehicleId, childSeat, supermarketStop, firstName, lastName, email, whatsapp, flightNumber, specialRequests, attribution, consent, website}`. Also `POST /api/bookings/{reference}/whatsapp` — a distinct, explicit server-side call to trigger the post-booking WhatsApp handoff (fired with `keepalive:true`, i.e. designed to survive page navigation/unload). |
| Booking reference format | **UNKNOWN/REVIEW REQUIRED** — the response shape returns a `.reference` field (used directly in the WhatsApp-trigger URL path), but no real booking was created during this read-only pass, so the exact format/prefix could not be observed. Do not assume it matches `FD-XXXXXX`. |
| Production data store(s)/bindings | UNKNOWN/REVIEW REQUIRED — no backing D1/KV/R2 could be identified without a source repo or a working Cloudflare mapping. |
| Vehicle taxonomy | `sedan`, `minivan`, `minibus` (identical label set to storefronts 1/2's core three, keyed by string in the quotes response). No boat option surfaced. Booking payload uses `vehicleId`, not a literal `vehicleType` string — **unconfirmed whether `vehicleId` values are these same three strings or an opaque numeric/UUID id**; the quote response's `fares` object keys are the only vehicle identifiers actually observed. |
| Route/location identifiers | A fixed, hub-and-spoke `destinationId` slug list (all implicitly ex-Nadi-Airport), materially **coarser** than storefronts 1/2's per-hotel zone model — many hotels are pre-grouped into one corridor: `nadi-airport-hotels`, `wailoaloa-nadi`, `hilton-denarau`, `sofitel-denarau`, `sheraton-denarau`, `radisson-denarau`, `port-denarau`, `sonaisali`, `vuda-lautoka`, `marriott-momi`, `natadola-intercontinental`, `shangri-la-yanuca`, `outrigger-coral-coast`, `warwick-naviti`, `pearl-pacific-harbour`, `grand-pacific-suva`, `rakiraki-volivoli`. No free-text/custom-address path was found in the shipped bundle (no Places-autocomplete-style component referenced) — appears to be a closed, curated destination list only. |
| Fare calculation inputs | Opaque server-side — the client only sends `destinationId` + `tripType` + party size; there is no visible per-km/base-rate model in the shipped bundle (unlike storefronts 1/2, whose tiered `pricing_rules` are enforced server-side but whose *inputs* — real distance, fuel index — are documented). `pricingVersionId: "baseline-v1"` implies a versioned fixed-fare table server-side, but its contents/update mechanism are UNKNOWN/REVIEW REQUIRED. |
| Guest/contact fields | `firstName`, `lastName`, `email`, `whatsapp` — a first/last name split, unlike storefronts 1/2's single `guest_name` field. |
| Flight fields | `flightNumber` — same concept as storefronts 1/2, different casing convention (camelCase vs snake_case, expected given a different codebase). |
| Return-trip representation | `tripType: "return"` plus `returnDate`/`returnTime` on the same quote/booking call — conceptually equivalent to storefronts 1/2's model (single record, not a separate leg object), and the UI states a flat "5% off the second leg" return discount (vs. storefronts 1/2's 1.85× return multiplier, a different discount curve — **do not assume these produce the same return-trip price for an equivalent route**). |
| Brand/source attribution | **The most structured of all three storefronts** — every booking is submitted with a first-class `attribution` object: `{source, medium, campaign, landingPath, referrer}` (default `{source:"direct", medium:"none", campaign:"", landingPath:"/", referrer:""}` when nothing else is present), plus a `website` honeypot field and an explicit `consent` boolean. This is a materially better starting point for Issue #37's `BookingSource` contract than what storefronts 1/2 currently have (nothing structured at all) — worth adopting as the base shape for the canonical `BookingSource` contract rather than inventing a new one. |
| Current notification path | Explicit, decoupled `POST /api/bookings/{reference}/whatsapp` call, fired by the client immediately after a successful booking response, `keepalive:true`. Functionally similar intent to storefronts 1/2's optional guest WhatsApp continuation, but implemented as a real tracked server call rather than a client-only `wa.me` link — the server presumably logs or triggers something when this fires (UNKNOWN/REVIEW REQUIRED exactly what). The static config bundle also exposes `whatsappNumber: "61478886145"` — **the identical ops WhatsApp number used across the whole Vakaviti ecosystem** (confirmed, not inferred), which is the strongest concrete evidence found that this storefront is genuinely operated by the same business, despite its infrastructure/hosting being unlocated. |
| Current admin/ops handoff | UNKNOWN/REVIEW REQUIRED — no admin surface was found or tested (none was searched for, out of caution, since finding one would likely require guessing at URLs/paths on an unfamiliar host). |
| Exact rollback target | UNKNOWN/REVIEW REQUIRED — cannot state a rollback target for a deployment whose hosting mechanism has not been located. **This alone should block any cutover work involving this storefront until resolved** — Issue #36's release constitution requires a known rollback target before any change, and none exists yet for this property. |
| Current live booking dependency chain | Best-effort reconstruction from black-box + bundle inspection only: guest picks a destination (Nadi Airport implicit origin) → `POST /api/quotes` locks a fare for ~30 min → guest fills contact/flight/vehicle details → `POST /api/bookings` (idempotent via a client-generated UUID, tied to the locked `fareQuoteId`) → `POST /api/bookings/{reference}/whatsapp` fires the ops handoff. No admin visibility, data store, or rollback path could be confirmed. |

## Live fare comparison captured this pass (read-only `POST /api/quotes`, one-way, 2 pax/2 bags, 2026-09-10)

| Destination (bft slug) | sedan | minivan | minibus |
|---|---|---|---|
| hilton-denarau | 49 | 69 | 99 |
| port-denarau | 39 | 69 | 99 |
| wailoaloa-nadi | 39 | 55 | 79 |
| sonaisali | 69 | 89 | 119 |
| outrigger-coral-coast | 159 | 199 | 249 |
| natadola-intercontinental | 139 | 175 | 219 |
| marriott-momi | 119 | 149 | 189 |
| pearl-pacific-harbour | 269 | 329 | 419 |
| grand-pacific-suva | 329 | 399 | 499 |

See the main report's shadow-comparison table for variance against storefronts 1/2's authoritative reference fare for the equivalent routes, and `vakaviti-core/shadow-log/` for the full structured records.

**Headline promo observed on the live homepage:** "Nadi Airport to Port Denarau, FJ$39 total, sedan only, 1–3 pax" — explicitly framed as a launch special. This is the most likely explanation for `port-denarau`'s sedan fare (39) being lower than `hilton-denarau`'s (49) despite both being the same physical corridor on storefronts 1/2 — classified as a **promotional fare**, not a routing error (see main report's mismatch classification).
